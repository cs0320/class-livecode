This repo contains the generated code from Nov 4, 2025. 

Note that not everything here is great! See the lecture video. In particular, 
the generated PBT test isn't ideal, because `==` isn't a safe way to compare 
two answers to the nearest-neighbor problem. 
